import { Typography } from '@mui/material';
import styles from './AdminContract.module.scss';

function AdminContract() {
  return (
    <div className={styles.container}>
      <Typography>{'typeContract'}</Typography>
    </div>
  );
}

export default AdminContract;
